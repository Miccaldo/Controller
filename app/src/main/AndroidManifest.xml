<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miccaldo.controller">

    <application
        android:name=".MyAppContext"
        android:allowBackup="true"
        android:icon="@mipmap/ic_spider"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_spider"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Connect"
            android:label="@string/title_activity_connect"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.miccaldo.controller.MainActivity" />
        </activity>
    </application>

</manifest>